name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/node@16.6
    - name: Install Eslint
      run: yarn add eslint
    - name: Run ESLint
      run: npx eslint . --ext .ts
