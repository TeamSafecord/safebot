{
  "scripts": {
    "watch": "tsc -w",
    "build": "tsc && copyfiles .env package.json yarn.lock dist"
  },
  "dependencies": {
    "copyfiles": "^2.4.1",
    "discord.js": "^13.3.1",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.7",
    "typescript": "^4.4.4"
  }
}
